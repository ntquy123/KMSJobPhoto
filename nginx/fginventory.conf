map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 8081;
    listen [::]:8081;

    location / {
        root /usr/share/nginx/pkwms/;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://app:8081/;
        proxy_redirect off;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_cache off;
        proxy_buffering off;
        proxy_read_timeout 100s;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /swagger {
        alias /usr/share/nginx/pkwms/swagger-ui-5.30.2/dist/;
        index index.html;
    }

    location /swagger/devhelpers {
        alias /usr/share/nginx/pkwms/swagger-ui-5.30.2/dev-helpers/;
        index index.html;
    }

    location /goaccess {
        alias /usr/share/nginx/goaccess-1.5.6/resources/;
        index tpls.html;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
